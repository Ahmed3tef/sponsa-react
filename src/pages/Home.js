import React from 'react';

const Home = () => {
  const logoutHandler = () => {};
  return (
    <div>
      <button onClick={logoutHandler}>click here</button>
    </div>
  );
};

export default Home;
